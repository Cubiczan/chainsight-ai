DATABASE_URL=file:./db/custom.db
# Static API key required to call POST /api/analyze. Sent as `Authorization: Bearer <key>`.
# When unset, /api/analyze fails closed with 503 (never serves anonymously).
ANALYZE_API_KEY=
